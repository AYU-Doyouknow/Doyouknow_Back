spring.application.name=DoyouknowBack

# =========================
# Loadtest Switch (Seeder)
# =========================
loadtest.seed.enabled=false

# =========================
# MYSQL (????? DB? ??)
#  - ?? application.properties ? ???? fallback ??? ????,
#    loadtest??? ???? loadtest DB? ???
# =========================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/doyouknow_loadtest}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:0000}

# --- JPA ---
spring.jpa.hibernate.ddl-auto=update

# === JPA ??? ??? ? true ? ?? === #
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
# ==================================== #

spring.jpa.hibernate.dialect=org.hibernate.dialect.MySQLDialect
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE


# =========================
# ????? ? ??? ??(??)
# - ????? ??? ??? ?? DB? ?? ??? ??? ? ??
# =========================
spring.task.scheduling.enabled=false

# --- Security ---
http.auth-token-header=Authorization
http.auth-token=${APP_AUTH_TOKEN:qweqwe}

# --- Swagger ---
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# --- Actuator ---
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=when_authorized
management.info.java.enabled=true
management.info.os.enabled=true
management.server.port=8081